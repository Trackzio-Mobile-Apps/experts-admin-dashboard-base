# Local development only. Copy to `.env.local` (never commit real values).
# Do NOT put secrets in git. Set production values in the Netlify dashboard.

# --- Brand this deploy (same codebase, different product) ---
# Defaults are Coinzy. Change these four to ship another app.
# See docs/branding-a-new-app.md
APP_NAME=Coinzy
APP_ICON=C
APP_COLOR=#7c3c3f
API_BASE_URL=https://api.coinzy-experts-qa.trackzio.com
# Optional: stable id (defaults from the name). Use when the Netlify branch name differs.
# APP_ID=coinzy
# Optional: sidebar hex (defaults to a slightly darker primary).
# APP_SIDEBAR=#823f42
# Optional: comma-separated labels under the sidebar logo.
# APP_MODELS=Coin evaluation

# Optional local reference only — operators enter this on the login page.
# Do NOT put ADMIN_API_KEY in Netlify env vars. It must match the API server's ADMIN_API_KEY.
# ADMIN_API_KEY=change-me

# Firebase web app config (from Firebase Console → Project settings → Your apps)
# Required keys for profile image upload.
FIREBASE_API_KEY=AIzaSyCso6kfwRQMrVxJUk5mZnYb5h1wQW0F3Pw
FIREBASE_AUTH_DOMAIN=coinzy-26a4d.firebaseapp.com
FIREBASE_PROJECT_ID=coinzy-26a4d
FIREBASE_STORAGE_BUCKET=coinzy-26a4d.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=1005300770086
FIREBASE_APP_ID=1:1005300770086:web:2127aa75e10300bdd9cde7
FIREBASE_MEASUREMENT_ID=G-936G54P2J8

# Optional: default inbox shown on Reports / Settings.
# REPORT_RECIPIENT_EMAIL=ops@example.com
